use crate::func::func;
use crate::variables::variables;
use crate::contflow::contflow;
use crate::loops::loops;
mod func;
mod variables;
mod contflow;
mod loops;
fn main() {
    //println!("Hello, world!");
    //variables();
    //func();
    //contflow();
    loops();    
}
